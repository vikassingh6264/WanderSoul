"""
Agent package — the decision-making brain of WanderSoul.

Contains the routing logic that inspects user context (budget, days,
interests, mobility needs) and explicitly chooses which capability
path to invoke. This decision logic is readable, testable, and NOT
hidden inside a single giant prompt string.
"""
